[package]
name = "weaven-debugger-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
weaven-core = { path = "../weaven-core", features = ["trace"] }
