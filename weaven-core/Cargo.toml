[package]
name = "weaven-core"
version = "0.1.0"
edition = "2021"

[features]
default = []
trace = []
parallel = ["dep:rayon"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = { version = "1", optional = true }
